<h1>Lista de posts:</h1>

{{#each}} {{!-- each serve "para cada psotagem faça algo" --}}
    <h1>{{titulo}}</h1>
    <p>{{conteudo}}</p>
    <hr>
    <a href="/deletar/{{id}}"><button>Deletar</button></a>

{{/each}}